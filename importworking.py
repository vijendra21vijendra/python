"""here we will learn that how import works
first it will see in the same directory and then at otherthings which are in path
import flask
and then print(flask.__path__) -> prints where it went to see

"""
# import sys
# import flask
# print(flask.__path__)

"""here we see what is happening is that we are able to import and as well as that program of fileq also runs so if we don't want that then
we will write that in the main method"""

import file1
print(file1.a)
print(file1.hardTime())